import random

user_pwLength = int(input())

choiceStr = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz1234567890!@#%!?/\-_~*^"

pwStr = ""

for i in range(user_pwLength):

	x = random.randrange(len(choiceStr))
	randomPick = choiceStr[x]
	pwStr = pwStr + randomPick

print(pwStr)
	
	
	
